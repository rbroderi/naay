# Installation

Install the documentation tooling (locally):

```bash
python -m pip install -r docs/requirements.txt
```

To install the Python package in editable mode for local API doc generation:

```bash
python -m pip install -e .
```
