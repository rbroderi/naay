# API Reference

The API reference is generated with mkdocstrings from the Python package. Make sure the package is installed in your environment (pip install -e .) so mkdocstrings can import it.

::: naay
